<Project Sdk="Microsoft.NET.Sdk">

    <PropertyGroup>
        <TargetFramework>netstandard2.0</TargetFramework>
        <RootNamespace>SQLite</RootNamespace>
        <PackageVersion>1.1.1</PackageVersion>
        <PackageLicenseUrl>https://github.com/chingham/SQLite.NetStandard/blob/master/LICENSE</PackageLicenseUrl>
    </PropertyGroup>

    <PropertyGroup>
    	<GeneratePackageOnBuild>true</GeneratePackageOnBuild>
    	<PackageId>SQLite.NetStandard</PackageId>
    	<Version>1.1.0</Version>
    	<Title>.NetStandard SQLite Abstraction</Title>
    	<Authors>Thomas Guzman</Authors>
        <PackageLicenseFile>LICENSE</PackageLicenseFile>
        <PackageProjectUrl>https://github.com/chingham/SQLite.NetStandard</PackageProjectUrl>
    </PropertyGroup>

    <ItemGroup>
    	<None Include="..\..\LICENSE" Pack="true" PackagePath="" />
    </ItemGroup>

</Project>
